# DVWA — vulnerable modules (denominator @ security=low). DVWA has no scoreboard, so
# "found" comes from the scanner's exported report (bench/results/dvwa.report.txt);
# each expected id is matched (case-insensitive substring) against the report lines.
#
# IMAGE (build 133+): ghcr.io/digininja/dvwa (native multi-arch, MariaDB split out) — replaced the
# deprecated amd64-only vulnerables/web-dvwa (qemu-emulated on arm64 → unstable under scan load). All 11
# modules below are PRESENT and reachable on this image, so max is now 11/11 (the old image LACKED the
# Open HTTP Redirect module → old real max was 10/11).
#
# The new image ALSO ships modules we deliberately EXCLUDE from the denominator as non-goals (per TODO §6):
#   cryptography (crypto-reversing), captcha (Insecure CAPTCHA — needs a real CAPTCHA/key), weak_id
#   (statistical session-id prediction), csp (CSP-bypass — client-side policy reasoning), javascript
#   (client-side challenge/obfuscation), api (token/OpenAPI-shaped, overlaps other classes). authbypass /
#   bac (Authorisation Bypass / Broken Access Control) are FAIR future candidates (access-control class,
#   akin to our BFLA work) but are NOT in the denominator yet — add them only with a zero-FP generic oracle.
SQL injection
SQL injection (blind)
Cross-site scripting (reflected)
Cross-site scripting (stored)
Cross-site scripting (DOM)
Command injection
File inclusion
File upload
Cross-site request forgery
Open redirect
Brute force
